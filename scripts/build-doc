#!/usr/bin/env node

/* eslint-env node */

require('./util/work-from-root')();

const docBuilder = require('./util/doc-builder')({});

console.log('Building ....\n');
docBuilder.build().then(() => {
  console.log('\nDone building');
}).catch(error => {
  console.error('\nError', error);
});
