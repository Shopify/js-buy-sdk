machine:
  node:
    version: v7.4.0
dependencies:
  pre:
    - 'echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > ~/.npmrc'
test:
  pre:
    - mkdir -p $CIRCLE_TEST_REPORTS/junit
  override:
    - yarn test
