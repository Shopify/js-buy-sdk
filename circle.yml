machine:
  node:
    version: 5.1.0

dependencies:
  pre: npm install -g npm@3.3.12

test:
  override:
    - npm run test-ci
    - mkdir -p $CIRCLE_TEST_REPORTS/xunit
    - mv tests.xml $CIRCLE_TEST_REPORTS/xunit/tests.xml
