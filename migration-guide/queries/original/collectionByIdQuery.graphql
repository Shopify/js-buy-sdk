# collection.fetch() - query collection by id

query ($id: ID!) {
  node(id: $id) {
    id
    handle
    description
    descriptionHtml
    updatedAt
    title
    image {
      id
      src: url
      altText
    }
  }
}
