fragment MetafieldFragment on Metafield {
    key
    namespace
    type
    value
    reference {
        ... on MediaImage {
            image {
               originalSrc
            }
        }
    }
}
